//User defined module.


const adding = (x,y) => x + y

const subtracting = (x,y) => x- y

const multiplying = (x,y) => x * y

const dividing = (x,y) => x / y


// Exporting the methods of the module 
module.exports = {adding, subtracting, multiplying, dividing}